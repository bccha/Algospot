#include <iostream>
#define MAX 987654321

using namespace std;

int main() {
	cout << fixed;
	cout.precision(10);
	cin.sync_with_stdio(false);
	cout.sync_with_stdio(false);

	int numTests;
	cin >> numTests;

	while (numTests--) {
	}
}
